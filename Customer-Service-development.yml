server:
  port: 5051
db:
  name: customerdb
spring:
  h2:
    console:
      enabled: true
  datasource:
    schema: customer
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/