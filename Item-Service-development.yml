server:
  port: 5052
db:
  name: itemdb
spring:
  h2:
    console:
      enabled: true
  datasource:
    schema: item
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
management:
  endpoints:
    web:
      exposure:
        include: '*'